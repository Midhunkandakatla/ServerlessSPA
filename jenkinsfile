pipeline {
    stages {
        stage ('Build') {
            steps {
                checkout SCM
            }
        }
        stage ('install') {
            steps {
                sh'npm install'
            }
        }
       stage ('build') {
            steps {
                sh'ng build'
            }
        }
    }
}
